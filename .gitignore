build

# Swift Package Manager, for the macOS helper (Package.swift at the root)
.build/
.swiftpm/

.idea/

# Raw output from tools/windows-checks - contains corporate hostnames, usernames,
# domain names and clipboard previews of internal documents. Never commit to a
# public repo; transcribe the findings into the issue by hand instead.
tools/windows-checks/deskhopplus-*.log
tools/windows-checks/deskhopplus-*.json

.DS_Store

# Personal Claude Code settings, not team-wide
.claude/settings.local.json

# Claude Code session working files: a resume checkpoint naming one session's
# id, and a lock holding a live pid. The skills beside them are team-wide and
# are tracked.
.claude/RESUME.md
.claude/scheduled_tasks.lock

# The measure-only ECDH image (#110, tools/board-checks/README.md)
build-bench/

# clangd (local dev): index cache, generated compile db symlink,
# and a config holding a machine-specific ARM toolchain path.
.cache/
compile_commands.json
.clangd
